# Senha da porta de acesso do deploy web (Vercel). Defina no painel da Vercel
# (Settings → Environment Variables) ou em um .env.local NÃO versionado.
# Se ficar em branco, a porta de senha é desativada (acesso livre) — ok p/ dev.
SITE_PASSWORD=
